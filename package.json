{
	"name": "nexus-rag-oracle",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"packageManager": "bun@1.3.11",
	"description": "Chatbot corporativo com RAG local (Ollama + Chroma)",
	"workspaces": [
		"server",
		"client"
	],
	"scripts": {
		"dev": "concurrently -n client,server -c green,blue \"bun run --filter nexus-rag-oracle-client build:watch\" \"bun run --filter nexus-rag-oracle-server dev\"",
		"build": "bun run --filter nexus-rag-oracle-client build && bun run --filter nexus-rag-oracle-server build",
		"ingest": "bun run --filter nexus-rag-oracle-server ingest",
		"docker:ingest": "docker compose up ingest --force-recreate --no-deps",
		"start": "bun dist/server/index.js",
		"test": "bun run --filter nexus-rag-oracle-server test && bun run --filter nexus-rag-oracle-client test",
		"format": "biome check --write --no-errors-on-unmatched ."
	},
	"devDependencies": {
		"concurrently": "^9.1.2",
		"@biomejs/biome": "^2.4.10"
	}
}
